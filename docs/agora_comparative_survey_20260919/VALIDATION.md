# Questionnaire verification

- All 37 candidate packets match deterministic extraction from frozen source records; all recorded source hashes agree.
- 41 model pairs and 10 architecture pairs; 82 formal codes and 8 separate pilot codes. Every formal participant sees 2 pairs per arm without repeating a pair.
- Each exact pair is shown in balanced A/B orientations; the first arm is balanced 41/41 across the formal cycle.
- Public HTML contains no model/method identities or decoding key and performs no network requests.
- Synthetic response tests verify identity decoding, tie = 0.5, abstention = missing, pilot exclusion, full planned-prompt coverage, and rejection of duplicate participant codes. Synthetic data exists only in temporary test directories.
- Firefox end-to-end test passes on desktop and 390 px mobile viewport: consent, required ratings, no preselected preference, interface-language switch preserving responses, all four pairs, downloaded JSON integrity, no automatic upload, and back/edit persistence.
- Pilot print packet compiles to 13 pages without overfull boxes, missing characters, or font warnings. All 4,042 extracted words are within page bounds. Cover, A/B evidence, and rating sheet visually inspected.
- Participant HTML SHA-256: `0443204f89161bb5e015fc97ebcdfc97cf6e76844bd63c8eac98cad502a90361`.
- There are no collected human responses, measured pilot completion times, or study results in this package. The 20–30 minute estimate still needs participant pilot calibration.
